# Environment configuration for Nolon Bot Docker deployment

# User configuration (matches host user to avoid permission issues)
USER_ID=1000
GROUP_ID=1000

# ROS2 configuration
ROS_DOMAIN_ID=0
ROS_LOCALHOST_ONLY=0

# Display configuration for GUI applications
DISPLAY=:0

# Gazebo configuration
GAZEBO_MASTER_URI=http://localhost:11345
GAZEBO_MODEL_PATH=/opt/ros_ws/src/nolon_bot_description

# Hardware acceleration (uncomment if you have NVIDIA GPU)
# NVIDIA_VISIBLE_DEVICES=all
# NVIDIA_DRIVER_CAPABILITIES=compute,utility,display